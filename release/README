#Генератор контрапункта 

для работы требуется установленный lilypond, компилируется на g++ версии 9.1.1 с использованием c++17 для поддержки inline переменных

#Компиляция:
`$ make`

#Пример использования: 
`$ cpmaker -i "c'4 f'4 e'4 c'4" -o output`
